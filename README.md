Implemented Helm templating to achieve environment parity. 
Using values-dev.yaml and values-prod.yaml, we can now scale the infrastructure (Replicas/Storage) dynamically without modifying the core template logic.
